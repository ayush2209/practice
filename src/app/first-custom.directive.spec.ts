import { FirstCustomDirective } from './first-custom.directive';

describe('FirstCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstCustomDirective();
    expect(directive).toBeTruthy();
  });
});
